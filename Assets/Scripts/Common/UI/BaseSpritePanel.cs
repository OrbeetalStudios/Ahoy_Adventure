using System.Collections.Generic;
using UnityEngine;

public class BaseSpritePanel : MonoBehaviour
{
    protected Vector3 spriteScale = new Vector3(1.3f, 1.1f, 1f);
    protected List<GameObject> images = new();
}